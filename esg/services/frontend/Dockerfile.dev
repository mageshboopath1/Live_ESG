# ============================================================================
# Frontend Development Dockerfile
# ============================================================================
# This Dockerfile is optimized for development with hot-reloading support.
# It uses Bun for fast dependency installation and development server.
# ============================================================================

FROM oven/bun:1-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package.json bun.lockb* ./

# Install dependencies
RUN bun install

# Copy application files (will be overridden by volume mounts in dev)
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start development server with hot-reload
CMD ["bun", "run", "dev", "--host", "0.0.0.0"]
